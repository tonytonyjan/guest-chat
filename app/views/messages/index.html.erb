<% @messages.each do |message| %>
<%= div_for message, class: 'row' do %>
  <div class="col-sm-2">
    <%= message_label message %>
  </div>
  <div class="col-sm-10 content"><%= raw RDiscount.new(message.content, :autolink).to_html %></div>
<% end %>
<% end %>